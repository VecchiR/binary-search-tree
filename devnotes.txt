I know I can .gitignore this file here, HOWEVER I need it synced
between 2 PCs I use to code... so, yeah, here it is
(OLÁ QUERIDOS BRs. Tamo junto, vamo que vamo, um bejo no coração)

FEITO:
    OK - até aqui, tudo 'funciona', mas SÓ CABE 1 KEY-VALUE PAIR em cada bucket
        OK - transformar cada bucket numa linked-list  p/ acomodar mais pares
        OK - refactor all functions based on this new behavior (buckets as linked-lists) -> using refactored linked list module
            OK - set(key, value) 
            OK - get(key) 
            OK - has(key)
            OK - remove(key) 
            OK - length() 
            OK - clear() 
            OK - keys() 
            OK - values() 
            OK - entries() 
    OK - GROWING. (capcity, load factor...)

Extra (for later):
- HashSet class? - behaves the same as a HashMap but only contains keys with no values (faz nova "hashset-linked-list" module?)